import styled from 'styled-components';
import { Grid } from '@mui/material';

export const StyledPostsPaginationContainer = styled(Grid)`
  && {
    width: 100vw;
    height: 20vh;
    display: flex;
    align-items: center;
    justify-content: center;

    svg {
      margin: 1em;
    }
  }
`;
